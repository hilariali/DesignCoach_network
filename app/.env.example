# Backend API URL (set this if your backend is not running on localhost:3001)
# VITE_API_URL=https://your-backend-url.com/api

# NOTE: The xAI/Grok API key is now handled SERVER-SIDE only.
# Set this on the server process, NOT in this .env file:
#   export XAI_API_KEY=your_xai_api_key_here
